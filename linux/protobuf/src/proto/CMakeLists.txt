project(proto)

file(GLOB PROTO_SRCS ${CMAKE_CURRENT_SOURCE_DIR}/src/*.cc)
file(GLOB PROTO_HDRS ${CMAKE_CURRENT_SOURCE_DIR}/src/*.h)

add_library(${PROJECT_NAME} STATIC ${PROTO_SRCS} ${PROTO_HDRS})
SET_TARGET_PROPERTIES (proto PROPERTIES OUTPUT_NAME "proto")

